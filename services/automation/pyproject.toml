[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-mobile-test-studio-automation"
version = "0.1.0"
description = "Local automation and agent service for AI Mobile Test Studio"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
